<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Lab2.ContentView">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="ContentView"
                   FontSize="36"
                   FontAttributes="Bold"
                   Margin="30, 40, 30, 10"
                   HorizontalOptions="Center" />

            <ContentView BackgroundColor="Azure"
                     Margin="24"
                     Padding="24"
                     HorizontalOptions="Fill"
                     VerticalOptions="CenterAndExpand">
                
                <Label Text="The ContentView (colored aqua in this example) might not seem very useful because it can have a single child (in this example a Label) and doesn't do much else. But ContentView is sometimes a convenient way of providing a background color or giving a little margin to its child through its own Padding property."
                       TextColor="DarkMagenta" />

            </ContentView>

            <Button Text="Back" Margin="30, 10, 30, 10" Clicked="MainScreen" FontSize="16"/>

        </StackLayout>
    </ContentPage.Content>
    
</ContentPage>